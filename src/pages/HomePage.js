import React from 'react';
import { ReactDOM } from 'react';   
import ListCarDs from '../components/ListCarDs';

function Home(){
    return (                        
        <ListCarDs />
    );
}

export default Home;